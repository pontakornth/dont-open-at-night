import { IStyle } from "fela";

export const Container = (): IStyle => ({
    display: "flex",
    height: "100%",
    width: "80%",
    maxWidth: "1200px",
    marginLeft: "auto",
    marginRight: "auto"
});
