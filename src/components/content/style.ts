import { IStyle } from "fela";

export const ContentLayoutStyle = (): IStyle => ({
    marginLeft: "auto",
    marginRight: "auto",
    padding: "1rem"
});

export const ContentNightStyle = (): IStyle => ({
    background: "#444",
    color: "white"
});
